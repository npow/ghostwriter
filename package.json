{
  "name": "auto-blogger",
  "version": "0.1.0",
  "private": true,
  "description": "Autonomous AI content engine with anti-slop quality gates",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "db:generate": "pnpm --filter @auto-blogger/database db:generate",
    "db:migrate": "pnpm --filter @auto-blogger/database db:migrate",
    "db:studio": "pnpm --filter @auto-blogger/database db:studio"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22.0.0"
  }
}
